name: Compile C Libraries for Linux
on: [push, workflow_dispatch]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Compile Libraries
        run: |
          # Compile Game, TicTacToe, and Minesweeper
          gcc -shared -o game.so -fPIC game.c
          gcc -shared -o tictactoe.so -fPIC tictactoe.c
          gcc -shared -o minesweeper.so -fPIC minesweeper.c

      - name: Commit and Push .so files
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Add all compiled shared objects
          git add game.so tictactoe.so minesweeper.so
          
          # Only commit if there are actual binary changes
          git commit -m "Update compiled Linux libraries (.so)" || echo "No changes to commit"
          git push
